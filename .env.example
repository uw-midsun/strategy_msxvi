# Local PostgreSQL Database Configuration
LOCAL_DB_HOST=localhost
LOCAL_DB_PORT=5432
LOCAL_DB_USER=postgres
LOCAL_DB_PASSWORD=your_local_password
LOCAL_DB_NAME=strategy_msxvi

# Cloud PostgreSQL Database Configuration (optional)
DB_HOST=your_cloud_host.com
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_cloud_password
DB_NAME=strategy_msxvi

# Solcast API Configuration (required for live/historical irradiance data)
SOLCAST_API_KEY=your_solcast_api_key_here

# Optional: Path to locations CSV for irradiance archive
# LOCATIONS_CSV=/path/to/locations.csv
